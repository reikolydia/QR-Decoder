# QR-Decoder

Python terminal based QR code decoder, or a URL-encoded string decoder. Works well with characters outside ascii, like CN chars. code39 barcodes may work occasionally

Uses these libraries:
pillow
rich
pyperclip
pyrxing